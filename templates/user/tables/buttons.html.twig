<a class="btn btn-light btn-outline-dark" href="{{ path('edit_review', {'reviewId': row.id}) }}">{{ 'form.buttons.edit'|trans }}</a>
<a class="btn btn-light btn-outline-dark" href="{{ path('delete_review', {'reviewId': row.id}) }}">{{ 'form.buttons.delete'|trans }}</a>
